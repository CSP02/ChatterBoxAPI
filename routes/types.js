/**
 * ? Component Types (might add other types too)
 */
class Types {
    constructor() {
        this.ComponentTypes = Object.freeze({
            EMBED: 1,
            IMAGE: 2,
            VIDEO: 3
        })
    }
}

module.exports = { Types }